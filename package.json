{
  "name": "expressjs-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "start:loc": "cp ./src/config/loc/.env .env && npm install && npm run start",
    "start:dev": "cp ./src/config/dev/.env .env && npm install && npm run start",
    "start:stg": "cp ./src/config/stg/.env .env && npm install && npm run start",
    "start:prod": "cp ./src/config/prod/.env .env && npm install && npm run start",
    "docs": "jsdoc -c ./jsdoc.json",
    "clean": "rm -r docs && rm -r node_modules && rm package-lock.json && rm .env"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.1.0",
    "nodemon": "^3.0.1",
    "uuid": "^9.0.1",
    "winston": "^3.10.0"
  },
  "devDependencies": {
    "jsdoc": "^4.0.2"
  }
}
